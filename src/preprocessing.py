import csv
import numpy as np

from constants import *

def normalize_lat_lon(lat_lon_tuples):


def preprocess_for_rnn(csv_file, num_timesteps=4, stride=1):
    with open(csv_file, "rb") as f:
        rows = csv.reader(f, delimiter=',')





if __name__ == "__main__":

